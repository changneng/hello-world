kind: pipeline
type: docker
name: create-preview

# test commit
steps:
- name: test
  image: alpine
  commands:
  - echo "Create preview environment"
tirgger:
  ref:
  - refs/pull/**
  event:
  - create
