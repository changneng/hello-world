kind: pipeline
type: docker
name: create-preview

# test commit 1
steps:
- name: test
  image: alpine
  commands:
  - echo "Create preview environment"
trigger:
  ref:
  - refs/pull/**
  event:
  - create
